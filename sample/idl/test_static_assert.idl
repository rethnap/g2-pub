
#include "common.idl"

static_assert(1 + 2 > 0, "hello");

//int32 a = 1 + 2 + 3;

int32 simple()
{
    for (int32 i = 1; i < 9; ++i) {
  //      printf("value of a: %d\n", a);
        printf("loop(%d)\n", i);
    }

    return 0;
}

static_assert(3, "ok");

// static_assert( 4 - 3 - 1, "bad");
 static_assert(1 + 4, "happy");
